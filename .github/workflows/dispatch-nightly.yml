name: Dispatch nightly refresh after workflow changes

# Recovery trigger: this follow-up change lands after the dispatcher already exists on
# master, guaranteeing that GitHub evaluates its push event and starts nightly.yml.
on:
  push:
    branches: [master]
    paths:
      - ".github/workflows/nightly.yml"
      - ".github/workflows/dispatch-nightly.yml"

permissions:
  actions: write
  contents: read

concurrency:
  group: dispatch-nightly-refresh
  cancel-in-progress: true

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Start the nightly workflow as workflow_dispatch
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run nightly.yml --ref master --repo "$GITHUB_REPOSITORY"
