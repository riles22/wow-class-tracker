# Withdrawn unenhanced v1 curation artifacts

Withdrawn on 2026-08-04 after the reviewed `midnight-s2-raid-catalyst-v2` campaign replaced the earlier unenhanced v1 curation review.

- 44 materialized v1 profiles and 72 v1 candidate-selection reports are retained here for recoverability.
- These artifacts were never used to accept scale-factor records.
- They are intentionally outside the active `profiles/` and `selections/` directories because no production manifest entry references them.
- The v2 replacements preserve generator-derived permanent gems and enchants, use disjoint artifact identities, and passed the hardened admission checks before the move.
